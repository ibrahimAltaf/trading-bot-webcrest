# Copy to .env.production before `npm run build` (or edit .env.production in repo).
#
# Live API (nginx /api on VPS). Example:
#   VITE_API_BASE_URL=http://YOUR_IP/api
# With HTTPS + domain later:
#   VITE_API_BASE_URL=https://api.yourdomain.com

VITE_API_BASE_URL=http://147.93.96.42/api
