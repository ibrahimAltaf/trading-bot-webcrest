# Same-origin: browser → http://YOUR_IP/api/... → nginx → gunicorn :8000
# Do NOT use :8000 or :6000 in the browser — only nginx /api proxy.
VITE_API_BASE_URL=/api

# Only if API is on a different host (same origin not possible):
# VITE_API_BASE_URL=https://trading-bot.webcrestllc.com/api
