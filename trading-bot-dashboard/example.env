# Production / remote API (full URL), e.g. VPS:
# VITE_API_BASE_URL=http://147.93.96.42/api
#
# Local dev: leave EMPTY — app uses relative /api + Vite proxy (no CORS).
# Do NOT set http://localhost/api (breaks when UI is opened as http://127.0.0.1:5173 or phone LAN IP).
VITE_API_BASE_URL=